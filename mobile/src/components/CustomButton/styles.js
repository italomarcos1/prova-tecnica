import styled from 'styled-components/native';
import { TouchableOpacity } from 'react-native';

export const MadeButton = styled(TouchableOpacity)`
  height: 23px;

  align-items: center;
  justify-content: center;
`;

export const TextButton = styled.Text`
  color: #292929;
  font-weight: bold;
  font-size: 16px;
`;
